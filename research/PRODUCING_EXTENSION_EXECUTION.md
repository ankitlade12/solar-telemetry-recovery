# Producing-system replication: execution record

The separately frozen system-10 replication is complete. All eleven cases produced 4,702,320 forecasts; verification included 192,700 visible-history checks. All eight principal analysis tables reproduced with identical CSV hashes. The [findings](PRODUCING_EXTENSION_FINDINGS.md) are incorporated into the current manuscript.

## Protocol and source qualification

The [protocol](PRODUCING_SYSTEM_EXTENSION_PROTOCOL.md) and `PRODUCING_EXTENSION_LOCK_V1.json` preserve the declaration made before replication model fitting and forecast scoring. The original 83-file lock remains unchanged; the extension lock covers 139 dependencies. Its pre-freeze test record reports 75 passing tests.

Qualification produced 2016 and 2017 inputs under both primary and minute-end alignment interpretations. Source-screen distributions were inspected after original-study results were exposed. The extension therefore adds a producing recorded regime under a separately declared design, not an untouched original test or an independent climate.

## Verification and analysis

The authoritative completed records are:

- [Run manifest](../runs/producing_extension_v1/manifest.json).
- [Verification](../runs/producing_extension_v1/verification.json).
- [Analysis manifest](../runs/producing_extension_v1/analysis/manifest.json).
- [Exact table reproduction](../runs/producing_extension_reproduced_v1/reproduction_comparison.json).
- [First performance inspection](PRODUCING_EXTENSION_PERFORMANCE_EXPOSURE.json).

`producing_extension_analysis_derivation.json` records the relationship between the original and extension analysis implementations. The shared estimands and common-calendar bootstrap remain unchanged; system-specific inference is not pooled.

## Reproduction

Use the commands in the [artifact guide](../ARTIFACT_GUIDE.md) to reproduce tables or perform a fresh refit. The public extension dependency view omits only the private planning document and authenticates the parent locks and all 138 scientific dependencies. Earlier staging failures and the successful dependency check remain under their recorded run paths.

The original execution and completion queue have finished. Old process identifiers and session locators are not active work instructions. Superseded distribution packages have been removed from the working tree; generate new local packages from current checked sources when needed.
